<%= form_with(model: tweet, local: false) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
        <% tweet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post %>
    <%= form.text_area :post %>
  </div>

  <div class="field">
    <%= form.label :ujs %>
    <%= form.check_box :ujs %>
  </div>

  <div class="actions">
    <%= form.submit data: {disable_with: 'submitting...'} %>
  </div>
<% end %>
